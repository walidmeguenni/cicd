version: '3.8'

services:
  nojoom:
    container_name: nojoom-cleint
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - '4173:4173'
    volumes:
      - .:/app
      - /app/node_modules  